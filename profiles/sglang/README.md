# SGLang profile

This profile is an explicit base for CUDA SGLang deployments. Supply a model that SGLang supports on your selected accelerator, then benchmark it against the other profiles. MLX support is not assumed by this CUDA profile.

```bash
MODEL=your-org/your-model ../../bootstrap.sh sglang
```
